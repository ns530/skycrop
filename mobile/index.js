/**
 * SkyCrop Mobile - Entry Point
 * 
 * @format
 */

import { registerRootComponent } from 'expo';
import App from './src/App';

// Register the root component for Expo
registerRootComponent(App);

