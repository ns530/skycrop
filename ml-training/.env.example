# Environment variables for ml-training package

# Path to dataset root. Expected structure:
# ${DATA_DIR}/raw/{images,masks}
# ${DATA_DIR}/tiles/{train,val}/{images,masks}
DATA_DIR=./data

# Path to store TensorBoard runs and checkpoints
RUNS_DIR=./runs

# Version used for exporting models into models/unet/${MODEL_VERSION}
MODEL_VERSION=1.0.0