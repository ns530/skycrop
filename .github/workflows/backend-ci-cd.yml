name: Backend CI/CD

on:
  push:
    branches: [ main ]
    paths: [ 'backend/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'backend/**' ]

jobs:
  ci:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgis/postgis:15-3.3
        env:
          POSTGRES_DB: skycrop_test
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_pass
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
    - uses: actions/checkout@v6
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json
    - name: Install dependencies
      run: cd backend && npm ci
    - name: Install PostgreSQL client
      run: sudo apt-get update && sudo apt-get install -y postgresql-client
    - name: Lint
      run: cd backend && npm run lint
    - name: Setup test database
      run: |
        PGPASSWORD=test_pass psql -h localhost -U test_user -d skycrop_test -c "CREATE EXTENSION IF NOT EXISTS postgis;"
        PGPASSWORD=test_pass psql -h localhost -U test_user -d skycrop_test -c "CREATE EXTENSION IF NOT EXISTS \"uuid-ossp\";"
      env:
        PGPASSWORD: test_pass
    - name: Test
      run: cd backend && npm test
      env:
        NODE_ENV: test
        JWTSECRET: test-secret-key
        DBHOST: localhost
        DBPORT: '5432'
        DBNAME: skycrop_test
        DBUSER: test_user
        DBPASSWORD: test_pass
        REDISURL: redis://localhost:6379
        REDISENABLED: 'false'
    # Build step skipped as backend is Node.js without build script

  cd:
    needs: ci
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - name: Deploy to Railway
      uses: railwayapp/actions-deploy@v1
      with:
        railway-token: ${{ secrets.RAILWAY_TOKEN }}
        service: backend